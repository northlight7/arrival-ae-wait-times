"""Arrival engine test suite. See README.md in this directory.

A package (rather than bare test modules) so the regeneration entry points can
be run as `python -m tests.regen_golden` and `python -m tests.make_corpus_fixture`
from the engine/ directory.
"""
