#!/bin/bash
# LAUNCHER.command — double-click this on macOS to start Arrival.
# Works on both Apple Silicon and Intel Macs.
# On Windows, double-click LAUNCHER.bat instead.

cd "$(dirname "$0")"
exec bash LAUNCHER.sh
